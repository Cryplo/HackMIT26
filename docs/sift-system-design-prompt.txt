Use case: infographic-diagram
Create a polished, highly legible landscape system architecture image for this actual project, titled "Sift — System Design", subtitle "Reimbursement review • current implementation". High resolution, crisp typography, white/off-white background, light mint-green panels, dark forest-green text, restrained amber for human review. Professional technical diagram suitable for a hackathon presentation, flat vector-like artwork, generous whitespace, no decorative 3D. Aim for a wide 16:10 canvas. Clear labeled arrows, orderly grid, no crossing arrows or tiny illegible text.

Represent a SINGLE Next.js application, not microservices. Layout:
TOP horizontal client band titled "Web client · React + TypeScript" with four compact items "Submit claim", "Audit overview", "Reimbursements + search", "Investigation history". Connect bidirectionally to the server boundary with label "HTTP APIs · cached snapshots + polling".

CENTER large boundary titled "Next.js server · API routes + application services". Inside, primary flow left to right:
1. "Intake + extraction" with short lines "Validate uploads", "Private originals → structured evidence".
2. "Guarded assessment" with short lines "Amount · currency · date · policy", "Identity · merchant · duplicates".
3. "Decision routing" branches to a green small card "Eligible clean claims / Policy-cap auto-approval" and an amber card "Human review / Approve · reject · request evidence". Both feed "Saved decisions + notices" on right. Show approval and rejection as decisions, never payments.
Below assessment, a card "Automatic investigation" with short lines "Eligible unresolved evidence", "Bounded read tools + citations", "Reassess with findings", connected by a two-way loop to assessment. This is not web browsing.
Below human review, a card "Scoped feedback learning" with short lines "Private review reason", "Candidate → 12 safety cases → activation", "Recheck pending claims with their own evidence". Dashed green arrow from human review to learning and from learning back to assessment labeled "Tested evidence-check reuse". Short note under loop "Human decisions survive rechecks".

BOTTOM two visually distinct bands:
"External providers · server-side credentials" with 3 cards:
"Azure OpenAI / OpenAI" subtext "Receipt extraction";
"Jev · Gateway or TypeSafe" subtext "Bounded assessment + semantic search";
"Azure OpenAI" subtext "Investigation planner".
Use subtle labeled connector lines or matching numbered reference markers linking extraction to Azure/OpenAI, guarded assessment and client search through server to Jev, and investigation to Azure. Avoid suggesting browser calls providers directly.
"Persistence + delivery" with cards:
"Live: Supabase" subtext "Claims · evidence · runs · decisions · learned checks" and "Private receipt storage";
"Demo: private local files" subtext "Isolated store + simulated providers";
"Applicant notices" subtext "Preview by default · optional Resend outbox". Connect saved decisions to applicant notices. Link server boundary to persistence with simple shared vertical connector.
Footer in readable small text: "Synthetic data only • No authentication or payment execution • Background work runs in the server lifecycle".
Accuracy constraints: Live Supabase and simulated local mode are alternatives, not replicated stores. Do not add a queue service, vector database, Elasticsearch, web crawler, authentication provider, payment service, fine-tuning, accuracy metrics, or savings claims. Internal review reasons never go to applicant email. Limit text to the labels above, preserve meaning and spellings. Main focus is readable system structure and feedback loop, with provider and storage details subordinate.
